export const SCHEMA_NAME_PROPERTY = "x-fastify-schemaName" as const;

/**
 * The only JSON schema we recognize out of the box. This could change later if you
 * have something weird. PRs welcome.
 */
export const APPLICATION_JSON = "application/json" as const;
